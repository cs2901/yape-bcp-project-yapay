android {
            defaultConfig {
                applicationId = "org.nativescript.plugindemo.barcodescanner"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }